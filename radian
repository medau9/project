from math import pi #Der wert "pi" wird aus importiert
radian = float(input('Your radian:')) #es wird nach einer eingabe gefragt, die für die weitere rechnung gebraucht wird
degree = radian * (180/pi) #rechnung von Bogenmaß in Gradmaß

print(degree) #Das Ergebnis wird ausgegeben
